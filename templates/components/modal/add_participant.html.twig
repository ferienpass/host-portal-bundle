{% embed '@FerienpassHostPortal/components/modal.html.twig' with { id: id } %}
    {% block form %}
        {{ form_start(addParticipant) }}
        <div class="sm:flex sm:items-start">
            <div class="mx-auto flex-shrink-0 flex items-center justify-center h-12 w-12 rounded-full bg-green-100 sm:mx-0 sm:h-10 sm:w-10">
                <span class="block h-6 w-6 text-green-600">{{ source('@FerienpassHostPortal/icons/user-add.svg') }}</span>
            </div>
            <div class="mt-3 text-center sm:mt-0 sm:ml-4 sm:text-left">
                <h3 class="text-lg leading-6 font-medium text-gray-900" id="modal-headline-{{ id }}">Teilnehmer:in
                    hinzufügen</h3>
                <div class="mt-2">
                    <p class="text-sm leading-5 text-gray-500">
                        Füllen Sie die Felder aus, wenn Sie die Teilnahmeliste ergänzen wollen.
                    </p>
                </div>
                {% do addParticipant.submit.setRendered %}
                <div class="mt-8 mb-4 grid grid-cols-2 row-gap-2 col-gap-6 text-left">
                    {{ form_rest(addParticipant) }}
                </div>
            </div>
        </div>
        <div class="mt-5 sm:mt-4 sm:flex sm:flex-row-reverse">
            <button
                    class="relative shadow-sm inline-flex px-4 py-2 items-center border border-transparent text-sm leading-5 font-medium rounded-md focus:outline-none focus:shadow-outline mt-3 flex w-full justify-center text-base sm:text-sm sm:mt-0 sm:ml-4 sm:w-auto bg-green-600 text-white hover:bg-green-500 focus:border-green-700 focus:shadow-outline-green"
                    type="submit">Teilnehmer hinzufügen
            </button>

            <button
                    class="relative shadow-sm inline-flex px-4 py-2 items-center border border-transparent text-sm leading-5 font-medium rounded-md focus:outline-none focus:shadow-outline-blue bg-white border-gray-300 text-gray-700 hover:text-gray-500 focus:outline-none focus:border-blue-300 focus:shadow-outline mt-3 flex w-full justify-center text-base sm:text-sm sm:mt-0 sm:w-auto"
                    @click.prevent="modal = null">Abbrechen
            </button>
        </div>
        {{ form_end(addParticipant) }}
    {% endblock %}
{% endembed %}
